feat: 补全集合操作矩阵并实施 DT/D 极简重构

- 引入包级 `Maxmin` 和 `Near` 方法，通过符号化操作 (`>`, `<`) 实现高效的时间极值与临近搜索。
- 全面实施 `DT` (DateTime) 与 `D` (DateOnly) 缩写重构，极大提升常量池的视觉密度与编码效率。
- 优化解析引擎逻辑，采用 “.999 归一化” 策略，通过单布局 (`DTNs`) 覆盖全精度纳秒输入，大幅精简 `Formats` 列表。
- 实现万能同级判断 `IsSame`，完美支持跨精度的级联对齐比较。
- 补齐 `Milli`、`Micro`、`Nano` 语义化访问器，并同步更新测试用例矩阵。
- 完善 ISO8601 全精度系列及 Web 标准（Atom, RSS）布局常量。
